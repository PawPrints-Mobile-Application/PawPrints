import Accounts from "./Accounts";
import LoginHistory from "./LoginHistory";

const models = [Accounts, LoginHistory];

export { Accounts, LoginHistory };

export default {
  name: 'Temps',
  models: models
};
